## TODO 


1. come up with a way to delete the x402 fetch tool  (and x402 rpc tool? ) and just use x402Post 
  - need a way to pass in register values or use them as env values to avoid hallucinations 
  - really would love to get rid of the custom config for the swap quote and describe it in a skill 